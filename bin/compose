#!/usr/bin/env node

const composer = require('../composer')()

if (process.argv.length !== 4) {
    console.error('Usage: compose <composition> <composition.js>')
} else {
   eval(require('fs').readFileSync(process.argv[3], { encoding: 'utf8' })).deploy(process.argv[2])
}
